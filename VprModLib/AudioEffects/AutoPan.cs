using VprModLib.AudioEffects;

namespace VprModLib.AudioEffects
{
    // Align with param definition list index.
    public enum AutoPanParameterName
    {
        
        DEPTH = 0,
        LFO_FREQ = 1,
        SHAPE_TYPE = 2,
        SYNC_NOTE = 3,
        TEMPO_SYNC = 4,
    }
}