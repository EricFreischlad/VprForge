namespace VprModLib.AudioEffects
{
    public enum ReverseParameterName
    {
        /// <summary>
        /// If set to 1, the wave is reversed. The effect is not serialized if the wave is not reversed.
        /// </summary>
        REVERSE_WAVE = 0,
    }
}