namespace VprModLib.MidiEffects
{
    public enum RobotVoiceParameterName
    {
        /// <summary>
        /// The type of inorganic pitch correction to use. Modes are detailed in <see cref="RobotVoiceMode"/>.
        /// </summary>
        MODE = 0,
    }
}